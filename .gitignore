node_modules
HELP.md
target/
docs/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

*.properties

# Test configuration holds no secrets and must be committed: CI checks out a tree with no
# application.properties at all, so without this the test suite runs on the inline @Value
# defaults and behaves differently there than on a developer machine.
!src/test/resources/application.properties

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

lensbridge-dev.db

# Docker
.env
.env.*
!.env.example
docker/nginx/certs/*
!docker/nginx/certs/.gitkeep
.claude/worktrees/*

# Ticket-signing key material for tcketmanage-core. Never commit a private key —
# each environment generates its own (see tcketmanage.crypto.* in application-dev.properties).
**/keys/private.pem
**/keys/public.pem
