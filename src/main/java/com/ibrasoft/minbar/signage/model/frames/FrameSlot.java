package com.ibrasoft.minbar.signage.model.frames;

public enum FrameSlot {
    PRIMARY,   // main carousel area
    TICKER,    // bottom scroller / secondary
    SIDEBAR,   // persistent side panel
    OVERLAY    // modals / interruptions (e.g. next prayer countdown)
}
